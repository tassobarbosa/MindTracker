export enum HeadacheIntensity {
  None = 'none',
  Low = 'low',
  Medium = 'medium',
  High = 'high',
}

export enum WorkEnvironment {
  Office = 'office',
  HomeOffice = 'home_office',
  RemoteSpot = 'remote_spot',
  NoWork = 'no_work',
}